#define INTIN(x) int x; scanf("%d",&x);
#define INTARRIN(x,y) int x[y];
#define INT2ARRIN(x,y,z) int x[y][z];
#define STRIN(x) string x; cin >> (x);
#define STR2ARRIN(x,y,z) string x[y][z];
#define OUT(x) cout << (x);
#define PRINT(x) cout << (x) << "\n";
#define ASS(x,y) (x)=(y);
#define IF(x,y) if(x){y}
#define FOR(i,x,y) for (int i=(x);i<(y);i++)
#define REP(i,y) FOR(i,0,y)
#define PI (acos(-1))
#define SQR(X) ((X)*(X))
#define CUBE(X) ((X)*(X)*(X))
#define RET(x) return x;